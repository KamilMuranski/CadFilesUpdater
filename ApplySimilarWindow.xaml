<Window x:Class="CadFilesUpdater.Windows.ApplySimilarWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Apply to all attributes" Height="320" Width="720" MinHeight="280" MinWidth="640"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize">
    <Window.Resources>
        <Style x:Key="IconButtonStyle" TargetType="Button">
            <Setter Property="Padding" Value="14,8"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="MinWidth" Value="120"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8" Padding="2">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <Grid Margin="12">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" BorderBrush="#D1D5DB" BorderThickness="1" CornerRadius="6" Padding="10" Margin="0,0,0,10">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel>
                        <TextBlock Text="Target:" FontWeight="Bold" Margin="0,0,0,6"/>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,2">
                            <TextBlock Text="Block: " Foreground="Gray"/>
                            <TextBlock x:Name="BlockNameText" Text="(not set)"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Attribute: " Foreground="Gray"/>
                            <TextBlock x:Name="AttributeTagText" Text="(not set)"/>
                        </StackPanel>
                    </StackPanel>
                    <Button Grid.Column="1"
                            Content="Choose attribute..."
                            Click="ChooseAttribute_Click"
                            Background="#FFD700"
                            Style="{StaticResource IconButtonStyle}"
                            Margin="10,0,0,0"
                            VerticalAlignment="Center"/>
                </Grid>
            </Border>

            <TextBlock Grid.Row="1" Text="Value:" FontWeight="Bold" Margin="0,0,0,6"/>
            <TextBox x:Name="ValueBox" Grid.Row="2" MinWidth="520" Padding="6" />

            <DockPanel Grid.Row="3" Margin="0,12,0,0" LastChildFill="False" VerticalAlignment="Top">
                <Button Content="Apply changes in selected files"
                        Click="ApplySelected_Click"
                        Background="#065F46"
                        Style="{StaticResource IconButtonStyle}"
                        Margin="0,0,10,0"/>
                <Button Content="Apply changes in all files"
                        Click="ApplyAll_Click"
                        Background="#16A34A"
                        Style="{StaticResource IconButtonStyle}"
                        Margin="0,0,10,0"/>
                <Button Content="Cancel"
                        Click="Cancel_Click"
                        Background="#6B7280"
                        Style="{StaticResource IconButtonStyle}"/>
            </DockPanel>
        </Grid>
    </ScrollViewer>
</Window>

