<Window x:Class="CadFilesUpdater.Windows.ApplySimilarWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Apply to all attributes" Height="320" Width="720" MinHeight="280" MinWidth="640"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize">
    <Window.Resources>
        <Style x:Key="IconButtonStyle" TargetType="Button">
            <Setter Property="Padding" Value="14,8"/>
            <Setter Property="Margin" Value="0,0,8,8"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="MinWidth" Value="120"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4" Padding="2">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#F5F5F5"/>
                    <Setter Property="BorderBrush" Value="#0078D4"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <Grid Margin="12">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" BorderBrush="#D1D5DB" BorderThickness="1" CornerRadius="6" Padding="10" Margin="0,0,0,10">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel>
                        <TextBlock Text="Target:" FontWeight="Bold" Margin="0,0,0,6"/>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,2">
                            <TextBlock Text="Block: " Foreground="Gray"/>
                            <TextBlock x:Name="BlockNameText" Text="(not set)"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Attribute: " Foreground="Gray"/>
                            <TextBlock x:Name="AttributeTagText" Text="(not set)"/>
                        </StackPanel>
                    </StackPanel>
                    <Button Grid.Column="1"
                            Click="ChooseAttribute_Click"
                            Style="{StaticResource IconButtonStyle}"
                            Margin="10,0,0,0"
                            VerticalAlignment="Center">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE71C;" Margin="0,0,8,0" Foreground="#FFD700"/>
                            <TextBlock Text="Choose attribute..."/>
                        </StackPanel>
                    </Button>
                </Grid>
            </Border>

            <TextBlock Grid.Row="1" Text="Value:" FontWeight="Bold" Margin="0,0,0,6"/>
            <TextBox x:Name="ValueBox" Grid.Row="2" MinWidth="520" Padding="6" />

            <DockPanel Grid.Row="3" Margin="0,12,0,0" LastChildFill="False" VerticalAlignment="Top">
                <Button Click="ApplySelected_Click"
                        Style="{StaticResource IconButtonStyle}"
                        Margin="0,0,10,0">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE8B5;" Margin="0,0,8,0" Foreground="#065F46"/>
                        <TextBlock Text="Apply changes in selected files"/>
                    </StackPanel>
                </Button>
                <Button Click="ApplyAll_Click"
                        Style="{StaticResource IconButtonStyle}"
                        Margin="0,0,10,0">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE8B5;" Margin="0,0,8,0" Foreground="#16A34A"/>
                        <TextBlock Text="Apply changes in all files"/>
                    </StackPanel>
                </Button>
                <Button Click="Cancel_Click"
                        Style="{StaticResource IconButtonStyle}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE711;" Margin="0,0,8,0" Foreground="#6B7280"/>
                        <TextBlock Text="Cancel"/>
                    </StackPanel>
                </Button>
            </DockPanel>
        </Grid>
    </ScrollViewer>
</Window>

