<Window x:Class="CadFilesUpdater.Windows.ProgressWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Processing Files" Height="150" Width="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" 
                   Text="Processing files, please wait..." 
                   FontSize="14" 
                   FontWeight="Bold"
                   Margin="0,0,0,15"/>
        
        <ProgressBar x:Name="ProgressBar" 
                     Grid.Row="1"
                     Height="25"
                     Margin="0,0,0,15"/>
        
        <TextBlock x:Name="StatusText" 
                   Grid.Row="2"
                   Text="Preparing..."
                   FontSize="12"
                   HorizontalAlignment="Center"/>
    </Grid>
</Window>
